buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.1' //change this with latest gradle version.
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}