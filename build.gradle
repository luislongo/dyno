buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.2' //change this with latest gradle version.
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}